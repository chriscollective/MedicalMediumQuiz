# 管理員腳本使用說明

## 🔒 安全注意事項

以下腳本包含敏感資訊（帳號密碼），已被加入 `.gitignore` 不會上傳到 GitHub：
- `reset-admin.ts` - 重置管理員帳號
- `seed-admin.ts` - 初始化管理員帳號
- `create-admin.ts` - 互動式建立管理員帳號

**這些檔案僅保留在本地，不會被 git 追蹤。**

## 📝 可用的管理腳本

### 1. 重置管理員帳號
刪除所有現有管理員並創建新帳號：
```bash
npx tsx server/src/scripts/reset-admin.ts
```

### 2. 初始化管理員帳號 (首次設定)
檢查並創建預設管理員帳號（如果不存在）：
```bash
npx tsx server/src/scripts/seed-admin.ts
```

### 3. 互動式建立管理員
通過命令行互動式輸入建立新管理員：
```bash
npx tsx server/src/scripts/create-admin.ts
```

## 🔧 其他工具腳本

### 檢查資料庫狀態
```bash
npx tsx server/src/scripts/check-db.ts
```

### 列出所有題目
```bash
npx tsx server/src/scripts/list-questions.ts
```

### 清除測驗記錄
```bash
npx tsx server/src/scripts/clear-quizzes.ts
```

## ⚠️ 部署注意事項

1. 在生產環境部署前，請確保：
   - 已修改管理員帳號密碼（不使用預設值）
   - `.env` 檔案已正確配置
   - 敏感腳本檔案不會被上傳到版本控制

2. 如需在伺服器上設置管理員帳號：
   - 手動在伺服器上創建 `reset-admin.ts`
   - 或使用 `create-admin.ts` 互動式建立
   - 執行完畢後可刪除該腳本

3. 建議使用環境變數來管理初始管理員資訊，而不是硬編碼在腳本中。
